# Nadar Server Environment Configuration
# Copy this file to .env and fill in your actual values

# === REQUIRED ===

# Google Gemini API Key (required for vision and TTS)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# === OPTIONAL ===

# Server Configuration
# Port for the server to listen on (default: 4000)
PORT=4000

# Gemini API Timeouts
# Timeout for vision requests in milliseconds (default: 30000)
GEMINI_TIMEOUT_MS=30000
# Timeout for TTS requests in milliseconds (default: 20000)
GEMINI_TTS_TIMEOUT_MS=20000

# ElevenLabs TTS (optional fallback)
# If not provided, only Gemini TTS will be available
# Get from: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# === DEPLOYMENT/CI VARIABLES ===

# Git commit hash (used when git is not available in containers)
GIT_COMMIT=

# Build timestamp (used when git is not available in containers)
BUILD_TIME=

# === DEVELOPMENT NOTES ===

# The server will:
# - Bind to 0.0.0.0:PORT for LAN access from mobile devices
# - Enable CORS for cross-origin requests
# - Apply rate limiting (60 requests per minute)
# - Log requests with Morgan in combined format
# - Cache images for 2 minutes for follow-up requests

# For mobile development:
# - Ensure your computer and mobile device are on the same WiFi
# - The app will auto-discover the server using network scanning
# - Manual configuration is available in the app's Settings screen
